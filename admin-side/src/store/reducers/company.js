import { ERROR_MESSAGE } from "../actionType";

const initialState = {};
function companyReducer(state = initialState, action) {
  switch (action.type) {
    case ERROR_MESSAGE:
      break;

    default:
      break;
  }
}
